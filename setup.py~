try:
    from setuptools import setup
except ImportError:
    from distutils.core import setup

config = {
    'description': 'Skeleton project',
    'author': 'Jaime Ruiz',
    'url': 'http://ruizjaime.com',
    'download_url': 'http://ruizjaime.com',
    'author_email': 'jaimeruizno@gmail.com',
    'version': '0.1',
    'install_requires': ['nose'],
    'packages': ['NAME'],
    'scripts': [],
    'name': 'projectname'
}

setup(**config)
